import '../../styles/Cell.css'

export default function Cell({ row, col }) {

    return(
        <div className="cell">
            {/* <p className="cellPosition">{row},{col}</p> */}
        </div>
    );
}